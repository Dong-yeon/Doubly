/**
 * 레이아웃 상수 — 화면 간 리듬을 맞추는 값들.
 *
 * <p>실측에서 같은 역할의 값이 화면마다 갈렸다.
 * <ul>
 *   <li>스크롤 하단 여백: 120 · 100 · 96 · 48 · 32 · 없음 — 한 파일 안에서
 *       96 과 120 을 함께 쓰는 곳도 있었다(지도 모드에서 콘텐츠가 FAB 에 가림)</li>
 *   <li>리스트 카드 간격: 8 · 16 · 24 — 탭을 옮길 때마다 목록의 호흡이 바뀐다</li>
 *   <li>아이콘+텍스트 행의 아이콘 폭: 24~46 — 텍스트 시작점이 어긋난다</li>
 * </ul>
 */
import { spacing } from './spacing';

export const layout = {
  /** 화면 좌우 기본 패딩 */
  screenPadding: spacing.lg,

  /** 리스트 카드 사이 간격 */
  cardGap: spacing.sm,

  /** 섹션(카드 묶음) 사이 간격 */
  sectionGap: spacing.lg,

  /**
   * 스크롤 최하단 여백 — 하단에 떠 있는 FAB 이 마지막 항목을 가리지 않게.
   *
   * <p>440x956 에서 실측: FAB 상단이 y=859 라 <b>하단 97px 이 가려진다</b>.
   * 예전 값들은 96(1px 부족) · 100(3px 여유)이라 사실상 콘텐츠가 FAB 에 닿았다.
   * 120 은 23px 의 숨쉴 공간을 남긴다.
   */
  listBottomWithFab: 120,

  /** FAB 이 없는 화면의 스크롤 하단 여백 */
  listBottom: spacing.xl,

  /** 아이콘+텍스트 행에서 아이콘 컨테이너 고정 폭 — 텍스트 기준선을 맞춘다 */
  rowIcon: 40,

  /** 터치 타깃 최소 — iOS HIG 44pt / Android 48dp 중 큰 쪽에 맞추려면 48 */
  touchTarget: 44,

  /**
   * PC(웹) 앱 셸의 최대 폭 — 이보다 넓은 창에서는 이 폭으로 묶고 가운데 정렬한다.
   *
   * <p>기본 사용 형태는 <b>420~480px 의 작은 세로 창</b>이다(PC 카카오톡형,
   * docs/PC_APP_ANALYSIS_2026-09-14.md 6절 1번). 이 값은 "창을 넓혔을 때 폰 UI 가
   * 1400px 로 늘어나는 것"을 막는 상한선이지, 넓은 창을 겨냥한 설계가 아니다.
   *
   * <p><b>화면마다 다른 폭을 주지 않는다.</b> 채팅만 넓히자는 안이 있었지만
   * (분석 문서 4절 1단계 초안), 탭을 옮길 때마다 셸이 좌우로 출렁여 창 크기가
   * 바뀌는 것처럼 보인다. 한 값으로 고정하는 편이 창처럼 읽힌다.
   */
  shellMaxWidth: 640,

  /**
   * 세로 레일 폭 — 아이콘 24 + 라벨 11pt 가 들어가는 최소치.
   *
   * <p>레일이 켜지면 셸을 이 폭만큼 <b>넓혀서</b> 단다. 셸 안에서 레일이 자리를 빼앗으면
   * 콘텐츠가 552px 로 좁아져 `shellMaxWidth` 가 뜻하는 바가 흔들린다 — 레일이 있든 없든
   * 콘텐츠는 640 이다.
   *
   * <p>그래서 레일 조건은 "창 > shellMaxWidth + railWidth" 한 줄이다. 분석 문서 초안은
   * 1024px 를 적었지만, 셸이 640 으로 고정된 이상 창이 800 이든 1400 이든 콘텐츠 폭은
   * 같아서 그 중간에 또 하나의 경계를 두면 기준이 임의가 된다. 경계는 하나로 둔다 —
   * <b>작은 창이면 폰 레이아웃(하단 탭), 넓은 창이면 PC 창(레일)</b>.
   */
  railWidth: 88,
} as const;
