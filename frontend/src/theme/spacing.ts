/**
 * Fitto 간격/반경 토큰.
 *
 * 참고: 스펙 제안값(md:12, lg:16 …)보다 기존 앱 스케일을 유지한다.
 * 간격 스케일을 전역으로 좁히면 46개 화면의 레이아웃이 한꺼번에 재배치되어
 * (여백 붕괴/겹침) 시각 검증 없이 적용하기엔 위험하기 때문이다.
 * 프리미엄화의 핵심(컬러/폰트/컴포넌트)은 유지하면서 레이아웃 안정성을 지킨다.
 */
export const spacing = {
  /**
   * 라벨 바로 아래 미세 간격. xs(4)가 최소였던 탓에 그 아래가 필요할 때마다
   * `marginTop: 2` 리터럴로 빠져나갔고, 실측 39곳에서 같은 값이 반복됐다.
   * 스케일의 하한을 열어 리터럴로 도망갈 이유를 없앤다.
   */
  xxs: 2,
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
  xxl: 48,
} as const;

export const radius = {
  sm: 10,
  md: 14,
  lg: 16,
  xl: 24,
  /**
   * 완전히 둥근 모서리. `pill` 과 결과는 같지만, 원형 배지처럼 "지름의 절반"이
   * 아니라 "무조건 최대"를 뜻하는 자리에서는 999 리터럴이 쓰이고 있었다.
   * 두 관용구가 공존하지 않도록 이름을 준다.
   */
  full: 9999,
  pill: 100,
} as const;
