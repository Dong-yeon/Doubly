/** 운동 관련 공통 상수 — 종목 카탈로그 시드 데이터와 맞춘 자극 부위 목록. */
export const MUSCLE_GROUPS = ['가슴', '등', '어깨', '하체', '팔', '코어', '전신'];

/** 기구 칩 표시 순서 — 카탈로그에 실제로 있는 값만 이 순서로 골라 쓴다(나머진 뒤에 덧붙임). */
export const EQUIPMENT_ORDER = ['바벨', '덤벨', '머신', '케이블', '맨몸'];
