import type { RequestConfig } from './client';

/**
 * AI 호출 공통 옵션 — 타임아웃·재시도 정책을 <b>한 곳</b>에 모은다.
 *
 * <p><b>왜 75초인가</b>: 서버가 Gemini 를 기다리는 시간이 45초, 재시도까지 포함한 서버 예산이
 * 60초다(백엔드 {@code GeminiClient}). 예전엔 서버·프론트가 <b>둘 다 60초</b>라, 서버가
 * 60초를 꽉 채우는 순간 프론트도 정확히 같은 순간 abort 했다 — 서버는 계속 돌고 프론트는
 * 이미 포기한, 아무도 결과를 못 받는 상태다. 게다가 프론트가 먼저 끊으면 서버가 원인별로
 * 구분해 보낸 메시지("오늘 횟수를 다 썼어요" / "요청이 많아요")가 통째로 사라지고
 * "시간 초과" 하나로 뭉개진다. 서버 예산보다 넉넉히 위에 둬야 <b>서버가 먼저 지고 그 이유가
 * 프론트에 도착</b>한다.
 *
 * <p><b>왜 재시도 1회인가</b>: 서버는 생성 결과를 입력 해시 기준으로 캐시한다
 * (백엔드 {@code AiResultCache}). 그래서 프론트가 시간 초과로 끊었어도 서버 쪽 생성은
 * 끝나 캐시에 들어가 있는 경우가 많고, 그때 재시도는 <b>즉시</b> 성공한다.
 * 재시도가 없으면 그 결과를 눈앞에 두고 실패로 처리하는 셈이다.
 * (캐시를 쓰지 않는 음식 사진·운동 추천도 재시도가 손해는 아니다 — 같은 요청을 다시
 *  보내도 부작용이 없다.)
 */
export const AI_REQUEST: RequestConfig = {
  timeout: 75000,
  retryOnTimeout: true,
};
