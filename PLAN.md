# Fitto 기능 계획 (PLAN)

> 로드맵 단계별 진행 상태는 [README.md](README.md) 참고. 이 문서는 예정된 기능의 상세 스펙을 담는다.

---

## Feature: 커플 일상 피드 (Couple Feed — "우리 기록")

### 목표
커플의 하루가 자동으로 쌓이는 공유 타임라인. 이미 기록 중인 운동·식단·맛집 방문에
"일상 포스트(사진+글)"를 더해 하나의 피드로 보여준다. 비트윈류 앱의 핵심 리텐션 기능.

### 핵심 기능 (MVP)
1. 일상 포스트 — 사진(선택) + 글, 커플 공유. 글/사진 중 하나는 필수
2. 통합 타임라인 — 포스트 + 운동 완료 + 식단 기록 + 맛집 방문을 시간순 무한스크롤로
3. 이모지 반응 — 포스트에 이모지 토글(❤️🥰😆👍💪), 상대에게 푸시
4. 새 포스트 작성 시 상대에게 푸시 + 커플 채널(`/sub/couple/{id}`) FEED 이벤트로 실시간 갱신

### Non-goals (이번 MVP 제외)
- 댓글 (반응으로 대체, 대화는 채팅으로)
- 채팅 이미지의 피드 노출 (대화 맥락이라 노이즈)
- 운동/식단/방문 기록에 대한 반응 (포스트에만)
- 외부 공개·소셜, 스토리(24h) 형식

### API
- `GET  /api/v1/feed?cursor={ISO-datetime}&limit=20` — 통합 타임라인 (keyset 페이징, `nextCursor` 반환)
- `POST /api/v1/feed/posts` `{content?, imageUrl?}` — 포스트 작성
- `DELETE /api/v1/feed/posts/{id}` — 작성자 본인만
- `POST /api/v1/feed/posts/{id}/reactions` `{emoji}` — 반응 토글

타임라인 아이템 공통 형태: `{type: POST|WORKOUT|MEAL|PLACE_VISIT, refId, userId, userName, mine,
title, content?, imageUrl?, occurredAt, reactions?}` — 집계는 서비스 레이어에서 소스별
`created_at < cursor` 상위 N건을 모아 병합 정렬(별도 테이블 복제 없음).

### DB 스키마 (V9)

```sql
CREATE TABLE feed_posts (
    id         BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    couple_id  BIGINT NOT NULL REFERENCES relations (id),
    author_id  BIGINT NOT NULL REFERENCES users (id),
    content    TEXT,                 -- 글/사진 중 하나는 필수 (서비스 검증)
    image_url  VARCHAR(500),
    created_at TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE TABLE feed_reactions (
    id         BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    post_id    BIGINT      NOT NULL REFERENCES feed_posts (id) ON DELETE CASCADE,
    user_id    BIGINT      NOT NULL REFERENCES users (id),
    emoji      VARCHAR(10) NOT NULL,
    created_at TIMESTAMP   NOT NULL DEFAULT NOW(),
    UNIQUE (post_id, user_id, emoji)
);

CREATE INDEX idx_feed_posts_couple ON feed_posts (couple_id, created_at DESC);
```

### 화면 (frontend)
- `FeedScreen` — 홈 스택. 타임라인 무한스크롤, 타입별 카드(아이콘·색), 포스트에 반응 바
- `FeedComposeScreen` — 사진 선택(Cloudinary 업로드) + 글 작성. FAB "일상 남기기"로도 진입
- 홈 화면에 "우리 기록" 진입 카드

---

## Feature: 커플 여행 (Trip)

### 목표
커플이 함께 갈/다녀온 여행을 계획하고 기록하는 기능. 맛집 지도(places)의 확장 —
장소 핀을 여행 단위로 그룹핑하고, 일정(날짜 범위)과 D-day, 메모·커버 사진을 붙인다.

### 핵심 기능 (MVP)
1. 여행 생성/수정/삭제 — 제목, 시작/종료일, 메모, 커버 사진. 커플 둘 다 가능
2. 여행 목록 — 다가오는 여행 D-day, 여행 중/다녀옴 상태 구분
3. 장소 연결 — 맛집 지도의 장소를 여행에 담기/빼기. 여행 상세에 장소 목록 + 지도 핀
4. 여행 생성 시 상대에게 푸시 + 커플 채널 TRIP 이벤트

### Non-goals (이번 MVP 제외)
- ~~일자별 상세 일정표 (itinerary)~~ → 이후 구현됨 (아래 Trip Itinerary 참고)
- ~~카카오 플레이스 키워드 검색~~ → 이후 구현됨 (장소 추가 화면)
- 여행 회고 피드 포스트 연동 (피드에서 사진+글로 대체 가능)
- 경비 기록·정산

### API
- `POST /api/v1/trips` `{title, startDate, endDate, memo?, coverImageUrl?}` — 생성
- `GET  /api/v1/trips` — 목록 (placeCount 포함, startDate 최신순)
- `GET  /api/v1/trips/{id}` — 상세 (연결된 장소 목록 포함)
- `PUT  /api/v1/trips/{id}` — 부분 수정
- `DELETE /api/v1/trips/{id}` — 삭제 (장소는 연결만 해제)
- `POST /api/v1/trips/{id}/places/{placeId}` — 장소 담기
- `DELETE /api/v1/trips/{id}/places/{placeId}` — 장소 빼기

### DB 스키마 (V10)

```sql
CREATE TABLE trips (
    id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    couple_id       BIGINT       NOT NULL REFERENCES relations (id),
    title           VARCHAR(100) NOT NULL,
    start_date      DATE         NOT NULL,
    end_date        DATE         NOT NULL,   -- >= start_date (서비스 검증)
    memo            TEXT,
    cover_image_url VARCHAR(500),
    created_by      BIGINT       NOT NULL REFERENCES users (id),
    created_at      TIMESTAMP    NOT NULL DEFAULT NOW()
);

-- 장소를 여행으로 그룹핑 (여행 삭제 시 장소는 유지)
ALTER TABLE places ADD COLUMN trip_id BIGINT REFERENCES trips (id) ON DELETE SET NULL;

CREATE INDEX idx_trips_couple ON trips (couple_id, start_date DESC);
CREATE INDEX idx_places_trip ON places (trip_id);
```

### 화면 (frontend)
- `TripListScreen` — 맛집 탭 진입. 다가오는(D-N)/여행 중/다녀온 여행 카드
- `TripFormScreen` — 생성/수정 겸용 (제목·날짜·메모·커버 사진)
- `TripDetailScreen` — 여행 정보 + 담긴 장소 목록(지도 핀) + 장소 담기/빼기
- 맛집 지도 상단에 "✈️ 여행" 진입 칩

---

## Feature: 커플 여행 일자별 일정표 (Trip Itinerary) ✅

### 목표
트리플앱류의 핵심 기능. 여행(trip) 안에서 **Day별·시간순으로 일정을 배치**한다.
기존 Trip("장소를 담는 바구니")을 넘어, "1일차 오전 → 점심 → 오후"의 실제 동선을 만든다.
장소(place) 연결은 선택이라 저장 장소 없이도 "공항 도착" 같은 자유 항목을 넣을 수 있고,
연결하면 그 좌표가 Day 지도의 동선(번호 마커)에 나타난다.

### 핵심 기능 (MVP)
1. 일정 항목 CRUD — Day·시간(선택)·제목·종류(관광/식사/카페/이동/숙소)·메모. 커플 둘 다 가능
2. Day 그룹핑 — 여행 기간(startDate~endDate)만큼 1일차…N일차 자동 생성, 각 Day에 시간순 항목
3. 순서 변경 — 하루 안 ▲/▼ 이동(전체 재배치), Day 이동(수정 시 며칠차 재선택)
4. 장소 연결(선택) — 맛집 지도 장소를 항목에 연결하면 Day 지도에 순번 마커로 표시
5. **실시간 협업 편집** — 모든 항목 변경 시 커플 채널 `/sub/couple/{id}` TRIP 이벤트 발행
6. **AI 일정 생성** — 여행 제목(지역)·기간·저장 장소·요청사항을 Gemini 에 보내 Day 바이 Day
   일정을 받아 자동 채움(기존 일정 대체). `DateCourseService`(데이트 코스)의 여행 확장판

### Non-goals (이번 MVP 제외)
- 드래그 앤 드롭 정렬 (▲/▼ 버튼으로 대체 — 추가 네이티브 의존성 없이)
- 이동시간·경로 최적화 (AI 일정 생성으로 이후 확장)
- 시간 충돌 검사, 알림(리마인더)

### API (`/api/v1/trips/{id}`)
- `GET    /{id}` — 상세에 `days`(Day별 일정) + `places` 포함
- `GET    /{id}/items` — Day별 그룹만 조회
- `POST   /{id}/items` `{dayNo, placeId?, title, startTime?, category?, memo?}` — 추가(하루 맨 뒤)
- `PUT    /{id}/items/{itemId}` — 부분 수정(title·startTime·category·memo)
- `DELETE /{id}/items/{itemId}` — 삭제
- `PUT    /{id}/items/reorder` `{items:[{itemId, dayNo, sortOrder}]}` — 순서/Day 일괄 재배치
  - `{itemId}` 보다 리터럴 `reorder` 가 먼저 매칭됨
- `POST   /{id}/items/generate` `{preferences?}` — **AI 일정 생성**(기존 일정 대체), `days` 반환
  - Gemini 미설정 시 `AI_NOT_CONFIGURED`, 사용자별 일일 한도는 다른 AI 기능과 공유
  - 벌크 DELETE 후 `saveAll` 로 대체(삭제→삽입 순서 보장), 상대에게 푸시 + TRIP 이벤트

응답 형태: `TripDayResponse{dayNo, date, items[]}`,
`TripItemResponse{id, dayNo, sortOrder, startTime, title, category, memo, placeId, placeName, lat, lng, createdBy}`
— 연결 장소가 있으면 지도 표시용 좌표를 함께 싣는다.

### DB 스키마 (V18)

```sql
CREATE TABLE trip_items (
    id         BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    trip_id    BIGINT       NOT NULL REFERENCES trips (id) ON DELETE CASCADE,
    place_id   BIGINT       REFERENCES places (id) ON DELETE SET NULL,  -- 연결 장소(선택)
    day_no     INT          NOT NULL,           -- 1일차… (여행 기간 내, 서비스 검증)
    sort_order INT          NOT NULL,           -- 하루 안 순서(0부터)
    start_time TIME,                            -- 시간대(선택)
    title      VARCHAR(100) NOT NULL,
    category   VARCHAR(30),
    memo       TEXT,
    created_by BIGINT       NOT NULL REFERENCES users (id),
    created_at TIMESTAMP    NOT NULL DEFAULT NOW()
);
CREATE INDEX idx_trip_items_trip_day ON trip_items (trip_id, day_no, sort_order);
```

- 여행 삭제 시 `ON DELETE CASCADE` 로 항목 정리, 장소 삭제 시 `place_id` 만 NULL(항목은 유지).
- 기존 `places.trip_id`(바구니형 연결)는 "담긴 장소" 목록용으로 유지 — 일정표와 병행.

### 화면 (frontend)
- `TripDetailScreen` — **일정/장소 탭** 구조로 개편
  - 일정 탭: **✨ AI로 일정 짜기** → Day 선택 바 → 선택 Day 동선 지도(순번 마커) →
    시간순 항목 카드(▲/▼·수정·삭제) → "＋ 일정 추가"
  - 장소 탭: 담긴 장소 지도·목록 + "＋ 장소 담기"(기존 유지)
  - 추가/수정 모달: 며칠차·제목·시간·종류·메모(+추가 시 장소 연결)
  - AI 생성 모달: 요청사항(선택) 입력 + 대체 경고 + 생성 중 로딩(최대 1분)

### 이후 확장 (제안)
- 여행 앨범(feed_posts.trip_id) — 경비 정산·준비물 체크리스트는 아래에 구현됨

---

## Feature: 커플 여행 경비 정산 (Trip Expenses) ✅

### 목표
커플 여행에서 "누가 얼마 냈는지"를 기록하고 **반반 기준 정산액**("누가 누구에게 얼마")을
자동 계산한다. 트리플에도 가계부가 있지만, 커플은 **정산** 니즈가 훨씬 강하다.

### 핵심 기능 (MVP)
1. 경비 추가/수정/삭제 — 금액·낸 사람(커플 둘 중)·종류(식비/교통/숙박/쇼핑/관광/기타)·메모·며칠차(선택). 커플 둘 다 가능
2. 정산 요약 — 총 지출 / 내가 낸 돈 / 상대가 낸 돈 + **정산 방향·금액**(조회자 관점)
3. 경비 변경 시 커플 채널 TRIP 이벤트 발행(실시간 갱신)

### Non-goals (이번 MVP 제외)
- 환율·다통화 정산 (currency 컬럼만 두고 단일 통화로 합산 — 환율 후순위)
- 3인 이상 분할, 항목별 커스텀 분담 비율 (커플 반반 고정)
- 영수증 사진, 카드 연동

### API (`/api/v1/trips/{tripId}/expenses`)
- `GET    ` — 목록 + 정산 요약(조회자 관점)
- `POST   ` `{amount, paidBy?, currency?, category?, dayNo?, memo?}` — 추가(paidBy 미지정 시 호출자)
- `PUT    /{expenseId}` — 수정
- `DELETE /{expenseId}` — 삭제

정산 계산: `myPaid`/`partnerPaid` 합산 → `settlement{direction, amount}`.
`direction` = `SETTLED` | `PARTNER_OWES_ME`(내가 더 냄) | `I_OWE_PARTNER`(상대가 더 냄),
`amount` = |내지출 − 상대지출| ÷ 2 (반올림 2자리). `paidBy` 는 커플 두 사람만 허용.

### DB 스키마 (V19)

```sql
CREATE TABLE trip_expenses (
    id         BIGINT        GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    trip_id    BIGINT        NOT NULL REFERENCES trips (id) ON DELETE CASCADE,
    paid_by    BIGINT        NOT NULL REFERENCES users (id),   -- 낸 사람
    amount     NUMERIC(12,2) NOT NULL,
    currency   VARCHAR(3)    NOT NULL DEFAULT 'KRW',
    category   VARCHAR(30),
    day_no     INT,
    memo       VARCHAR(200),
    created_by BIGINT        NOT NULL REFERENCES users (id),
    created_at TIMESTAMP     NOT NULL DEFAULT NOW()
);
CREATE INDEX idx_trip_expenses_trip ON trip_expenses (trip_id, created_at DESC);
```

### 화면 (frontend)
- `TripExpenseScreen` — 요약 카드(총 지출·정산 배너·각자 지출) + 지출 내역 목록 + "＋ 경비 추가"
  - 추가/수정 모달: 금액·낸 사람(나/상대 토글)·종류 칩·메모
- `TripDetailScreen` 상단에 "💰 경비 정산" 진입 버튼

---

## Feature: 커플 여행 준비물 체크리스트 (Trip Checklist) ✅

### 목표
커플이 함께 여행 준비물을 적고 체크한다. **누가 챙겼는지(checkedBy)**를 남겨 둘이 나눠
준비하기 좋게 한다. 저비용·고리텐션 항목.

### 핵심 기능 (MVP)
1. 준비물 추가/이름수정/삭제 — 커플 둘 다 가능
2. 체크 토글 — 체크 시 체크한 사람 기록, 해제 시 비움. 진행 개수(체크/전체) 제공
3. 변경 시 커플 채널 TRIP 이벤트 발행(실시간 갱신)

### Non-goals (이번 MVP 제외)
- 준비물 템플릿·자동 추천, 카테고리 분류, 담당자 지정(체크한 사람으로 대체)
- 드래그 정렬 (추가 순서 유지)

### API (`/api/v1/trips/{tripId}/checklist`)
- `GET    ` — 목록 + 진행 개수(`total`, `checkedCount`)
- `POST   ` `{content}` — 추가(맨 뒤)
- `PUT    /{itemId}` `{content}` — 이름 수정
- `POST   /{itemId}/toggle` — 체크 토글
- `DELETE /{itemId}` — 삭제

### DB 스키마 (V20)

```sql
CREATE TABLE trip_checklist_items (
    id         BIGINT       GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    trip_id    BIGINT       NOT NULL REFERENCES trips (id) ON DELETE CASCADE,
    content    VARCHAR(200) NOT NULL,
    checked    BOOLEAN      NOT NULL DEFAULT FALSE,
    checked_by BIGINT       REFERENCES users (id),   -- 체크한 사람(미체크면 NULL)
    sort_order INT          NOT NULL DEFAULT 0,
    created_by BIGINT       NOT NULL REFERENCES users (id),
    created_at TIMESTAMP    NOT NULL DEFAULT NOW()
);
CREATE INDEX idx_trip_checklist_trip ON trip_checklist_items (trip_id, sort_order, id);
```

### 화면 (frontend)
- `TripChecklistScreen` — 진행률 바 + 인라인 추가 + 체크 토글(낙관적 반영) + 이름수정/삭제(길게 눌러)
  - 체크 시 "○○님이 챙김" 표시
- `TripDetailScreen` 상단에 "🧳 준비물" 진입 버튼(경비 정산과 나란히)

---

## Feature: 커플 맛집 지도 (Place Map)

### 목표
커플이 함께 방문한 맛집과 가고 싶은 장소를 공유 지도에 기록하는 기능.
기존 식단 기록(meals — 칼로리 분석)과 연동.

### 핵심 기능 (MVP)
1. 장소 핀 등록 — 방문완료 / 위시리스트 구분
2. 커플 공유 — 두 사람 모두 추가/수정 가능
3. 방문 기록 — 사진, 메모, 별점(1~5), 식단 기록(meal) 연동

### 스택
- 지도: 카카오맵 SDK (React Native)
- 백엔드: Spring Boot REST API
- DB: PostgreSQL (places, place_visits 테이블 추가)

### Non-goals (이번 MVP 제외)
- AI 데이트 코스 추천
- ~~외부 맛집 DB 연동 (네이버, 카카오 플레이스 검색)~~ → 카카오 플레이스 키워드 검색은 이후 구현됨 (장소 추가 화면, SDK services)
- 리뷰 공개/소셜 기능

### DB 스키마

```sql
CREATE TABLE places (
    id          BIGSERIAL PRIMARY KEY,
    couple_id   BIGINT REFERENCES relations(id),
    name        VARCHAR(100) NOT NULL,
    address     TEXT,
    lat         DECIMAL(10,7) NOT NULL,
    lng         DECIMAL(10,7) NOT NULL,
    category    VARCHAR(30),
    status      VARCHAR(20) DEFAULT 'wishlist', -- visited | wishlist
    added_by    BIGINT REFERENCES users(id),
    created_at  TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE place_visits (
    id          BIGSERIAL PRIMARY KEY,
    place_id    BIGINT REFERENCES places(id) ON DELETE CASCADE,
    visited_by  BIGINT REFERENCES users(id),
    visited_at  DATE NOT NULL DEFAULT CURRENT_DATE,
    rating      SMALLINT CHECK (rating BETWEEN 1 AND 5),
    memo        TEXT,
    image_url   TEXT,
    -- 원안은 food_logs(id) 참조였으나, 현재 코드베이스의 식단 테이블은 meals (V6__meals.sql)
    meal_id     BIGINT REFERENCES meals(id),
    created_at  TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_places_couple ON places(couple_id);
CREATE INDEX idx_places_location ON places(lat, lng);
```
