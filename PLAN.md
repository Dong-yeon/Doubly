# Fitto 기능 계획 (PLAN)

> 로드맵 단계별 진행 상태는 [README.md](README.md) 참고. 이 문서는 예정된 기능의 상세 스펙을 담는다.

---

## Feature: 커플 일상 피드 (Couple Feed — "우리 기록")

### 목표
커플의 하루가 자동으로 쌓이는 공유 타임라인. 이미 기록 중인 운동·식단·맛집 방문에
"일상 포스트(사진+글)"를 더해 하나의 피드로 보여준다. 비트윈류 앱의 핵심 리텐션 기능.

### 핵심 기능 (MVP)
1. 일상 포스트 — 사진(선택) + 글, 커플 공유. 글/사진 중 하나는 필수
2. 통합 타임라인 — 포스트 + 운동 완료 + 식단 기록 + 맛집 방문을 시간순 무한스크롤로
3. 이모지 반응 — 포스트에 이모지 토글(❤️🥰😆👍💪), 상대에게 푸시
4. 새 포스트 작성 시 상대에게 푸시 + 커플 채널(`/sub/couple/{id}`) FEED 이벤트로 실시간 갱신

### Non-goals (이번 MVP 제외)
- 댓글 (반응으로 대체, 대화는 채팅으로)
- 채팅 이미지의 피드 노출 (대화 맥락이라 노이즈)
- 운동/식단/방문 기록에 대한 반응 (포스트에만)
- 외부 공개·소셜, 스토리(24h) 형식

### API
- `GET  /api/v1/feed?cursor={ISO-datetime}&limit=20` — 통합 타임라인 (keyset 페이징, `nextCursor` 반환)
- `POST /api/v1/feed/posts` `{content?, imageUrl?}` — 포스트 작성
- `DELETE /api/v1/feed/posts/{id}` — 작성자 본인만
- `POST /api/v1/feed/posts/{id}/reactions` `{emoji}` — 반응 토글

타임라인 아이템 공통 형태: `{type: POST|WORKOUT|MEAL|PLACE_VISIT, refId, userId, userName, mine,
title, content?, imageUrl?, occurredAt, reactions?}` — 집계는 서비스 레이어에서 소스별
`created_at < cursor` 상위 N건을 모아 병합 정렬(별도 테이블 복제 없음).

### DB 스키마 (V9)

```sql
CREATE TABLE feed_posts (
    id         BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    couple_id  BIGINT NOT NULL REFERENCES relations (id),
    author_id  BIGINT NOT NULL REFERENCES users (id),
    content    TEXT,                 -- 글/사진 중 하나는 필수 (서비스 검증)
    image_url  VARCHAR(500),
    created_at TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE TABLE feed_reactions (
    id         BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    post_id    BIGINT      NOT NULL REFERENCES feed_posts (id) ON DELETE CASCADE,
    user_id    BIGINT      NOT NULL REFERENCES users (id),
    emoji      VARCHAR(10) NOT NULL,
    created_at TIMESTAMP   NOT NULL DEFAULT NOW(),
    UNIQUE (post_id, user_id, emoji)
);

CREATE INDEX idx_feed_posts_couple ON feed_posts (couple_id, created_at DESC);
```

### 화면 (frontend)
- `FeedScreen` — 홈 스택. 타임라인 무한스크롤, 타입별 카드(아이콘·색), 포스트에 반응 바
- `FeedComposeScreen` — 사진 선택(Cloudinary 업로드) + 글 작성. FAB "일상 남기기"로도 진입
- 홈 화면에 "우리 기록" 진입 카드

---

## Feature: 커플 맛집 지도 (Place Map)

### 목표
커플이 함께 방문한 맛집과 가고 싶은 장소를 공유 지도에 기록하는 기능.
기존 식단 기록(meals — 칼로리 분석)과 연동.

### 핵심 기능 (MVP)
1. 장소 핀 등록 — 방문완료 / 위시리스트 구분
2. 커플 공유 — 두 사람 모두 추가/수정 가능
3. 방문 기록 — 사진, 메모, 별점(1~5), 식단 기록(meal) 연동

### 스택
- 지도: 카카오맵 SDK (React Native)
- 백엔드: Spring Boot REST API
- DB: PostgreSQL (places, place_visits 테이블 추가)

### Non-goals (이번 MVP 제외)
- AI 데이트 코스 추천
- 외부 맛집 DB 연동 (네이버, 카카오 플레이스 검색)
- 리뷰 공개/소셜 기능

### DB 스키마

```sql
CREATE TABLE places (
    id          BIGSERIAL PRIMARY KEY,
    couple_id   BIGINT REFERENCES relations(id),
    name        VARCHAR(100) NOT NULL,
    address     TEXT,
    lat         DECIMAL(10,7) NOT NULL,
    lng         DECIMAL(10,7) NOT NULL,
    category    VARCHAR(30),
    status      VARCHAR(20) DEFAULT 'wishlist', -- visited | wishlist
    added_by    BIGINT REFERENCES users(id),
    created_at  TIMESTAMPTZ DEFAULT NOW()
);

CREATE TABLE place_visits (
    id          BIGSERIAL PRIMARY KEY,
    place_id    BIGINT REFERENCES places(id) ON DELETE CASCADE,
    visited_by  BIGINT REFERENCES users(id),
    visited_at  DATE NOT NULL DEFAULT CURRENT_DATE,
    rating      SMALLINT CHECK (rating BETWEEN 1 AND 5),
    memo        TEXT,
    image_url   TEXT,
    -- 원안은 food_logs(id) 참조였으나, 현재 코드베이스의 식단 테이블은 meals (V6__meals.sql)
    meal_id     BIGINT REFERENCES meals(id),
    created_at  TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_places_couple ON places(couple_id);
CREATE INDEX idx_places_location ON places(lat, lng);
```
