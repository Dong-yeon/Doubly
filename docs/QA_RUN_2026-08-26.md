# Doubly 실기기 QA 재검증 — 2026-08-26

> [QA_RUN_2026-08-25.md](QA_RUN_2026-08-25.md) 이후 하루 동안 대규모 코드 수정(QA_CHECKLIST.md
> 확정 결함 전체 + 전역 반복 패턴 1~10 + 트립/대결 진입 축소)이 있었던 것을 실제 Android
> 실기기(모델 10A30Q, jettalt)에서 재검증. 기존 QA-Tester-A/B 계정(`dubly.qatest1.20260825@
> gmail.com` 등) 재사용, Metro는 로컬 dev 서버(`adb reverse tcp:8081`)로 최신 커밋 번들을
> 강제 로드해 실행.

## 검증 범위와 우선순위

전날 QA 이후 변경 규모가 워낙 커서(패턴 1·3·4·6·7·8·9·10이 거의 전 화면에 걸쳐 30~79개
파일을 기계적으로 수정) 전 화면 재검증은 비현실적 — 실제 동작이 달라진 곳(1순위)과 이전
세션에서 아예 다루지 못했던 영역(2순위) 위주로 확인했다.

## 1순위 — 결과: 전부 정상

| 항목 | 결과 |
| --- | --- |
| 운동 세션 화면 (P0 드래그리스트 교체) | ✅ 운동 카드 정상 렌더링(드래그 핸들·대체종목·삭제·세트별 입력 전부). 세트 완료 체크 → 총 볼륨 반영 → 휴식 타이머 오버레이까지 정상 |
| 무게 필드 NaN 필터 (패턴 2) | ✅ `70.5.2` 입력 시 두 번째 `.`이 자동으로 걸러져 `70.52`로 유지됨 |
| 세션 이탈 가드 (패턴 3) | ✅ 세트 체크한 상태에서 뒤로가기 시 "세션 종료 — 기록하지 않고 나갈까요?" 다이얼로그 정상 |
| 휴식 타이머 -15/+15 버튼 (방금 추가한 접근성 라벨) | ✅ 레이아웃 정상, 라벨 추가로 인한 회귀 없음 |
| 여행 상세 — 경비/준비물 진입 숨김 | ✅ `TripDetailScreen`에서 두 버튼만 사라지고 앨범·회고 2개가 남은 줄이 자연스럽게 렌더링(47% flexBasis 그리드라 2개도 안 깨짐) |
| 여행 생성 → 캘린더 반영 | ✅ 날짜 선택(min-date 클램핑 포함) → "여행을 만들었어요" 토스트 → 캘린더 그리드에 기간 띠 → "우리 여행" 섹션 카드까지 정상 |
| 여행 삭제 (in-flight 가드, 패턴 7) | ✅ 길게 눌러 삭제 확인 다이얼로그 → "여행을 삭제했어요" → EmptyState 복귀 정상 |
| 운동 탭 "대결" 칩 숨김 | ✅ 몸 변화·음성 응원·통계·캘린더 4개만 남고 정상 렌더링 |
| 로그아웃 → 로그인 화면 전환 | ✅ 정상 전환. 스플래시 잔상 자체는 screencap 프레임 단위로 못 잡아 재현 확인은 못 했음(전날 문서에도 "100% 재현 안 됨, 타이밍 의존적 레이스"로 기록돼 있어 도구적 한계로 판단, 코드 수정은 이미 반영됨) |

## 2순위 — 전날 미검증 영역: 전부 정상 동작 확인

| 항목 | 결과 |
| --- | --- |
| 일상(피드) 글쓰기 | ✅ `FeedComposeScreen`에서 텍스트 입력 → "일상을 남겼어요" 토스트 → 홈 카드에 즉시 반영 |
| 터치 | ✅ "터치 보내기" 바텀시트(손잡기/토닥임/콕 찌르기/포옹/뽀뽀) → 전송 → 채팅방에 "손잡기" 리액션으로 실제 기록됨까지 확인 |
| 음성 응원 | ✅ `VoiceClipsScreen` 정상 렌더링(오늘의 부스터 + 5가지 순간별 녹음 카드). 실제 마이크 녹음까지는 미검증(권한/하드웨어 의존이라 스코프 밖) |
| 통화(음성) | ✅ 발신 UI(마이크 음소거·종료 버튼, "Calling...") 정상 → 종료 시 채팅방에 "부재중 전화 · 다시 걸기" 카드로 정상 기록. 상대 계정(B)이 동시 접속 상태가 아니라 실제 연결(수신~통화)까지는 미검증 |

## 확인하지 못한 것 (다음 라운드 후보)

- **로그아웃 스플래시 잔상 재현**: 도구(adb screencap)가 "한 프레임" 수준 레이스를 못 잡음. 코드 수정 자체(`skipSplash`/`everAuthenticatedRef`)는 이미 반영돼 있어 재현 실패가 곧 회귀는 아님.
- **통화 실제 연결(양방향)**: 기기가 하나뿐이라 QA-Tester-B를 동시에 붙여서 수신~연결까지는 확인 못 함.
- **음성 응원 실제 녹음/재생**: 마이크 권한·오디오 하드웨어 의존이라 화면 진입까지만 확인.
- 대결(챌린지)·여행 경비/준비물은 이번에 의도적으로 진입을 막았으므로 재검증 대상에서 제외.

## 정리

이번에 남긴 테스트 데이터(QATrip, QA test daily log entry 피드, 손잡기 터치, 부재중 전화
기록)는 세션 중 여행만 삭제로 정리했고, 나머지(피드 글·터치·통화 기록)는
[QA_RUN_2026-08-25.md](QA_RUN_2026-08-25.md)의 기존 방침대로 정리를 보류한다 — 다음에
운영 DB 정리할 때 한 번에 처리.
