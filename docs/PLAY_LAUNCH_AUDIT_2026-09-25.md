# Google Play 프로덕션 출시 요건 감사 (2026-09-25)

`FREE_TIER_AND_ADS.md` "Play Console 출시 — 남은 작업"(8/18)과 `STORE_LISTING_2026-09-17.md` §5 를
**저장소 근거**로 다시 확인했다. Play Console 은 이 세션에서 열 수 없으므로 각 항목을 "저장소가 말해주는 것"과
"콘솔에서 확인할 것"으로 나눈다. 결제 기능(오늘 병합)은 프로덕션 트랙이 아니면 실사용자가 쓸 수 없다.

## 항목별

| # | 요건 | 저장소 근거 (2026-09-25) | 콘솔에서 확인 |
| --- | --- | --- | --- |
| 1 | **비공개 테스트 20명 · 14일 연속** (개인 개발자 계정의 프로덕션 접근 요건) | 저장소에 테스터 명단·기간 기록 없음. `eas.json` 은 `track: production` 이고 `check-play-track.mjs` 로 트랙별 versionCode 를 볼 수 있다 | 테스트 → 비공개 테스트 → 테스터 수·시작일. 요건 충족 뒤 "프로덕션 액세스 신청" 상태 |
| 2 | 태블릿 스크린샷 (7인치 2장) | `store/compose.py` 가 `play-tablet7`(1200×1920) 을 만든다. `store/out/` 은 커밋하지 않는 폴더라 지금 없다. **`store/shots/01-photo.png` 가 아직 없어** 5장 중 4장만 합성된다 | 스토어 등록정보 → 태블릿 스크린샷 칸이 비어 있는지 |
| 3 | OAuth(구글 로그인) 체크박스 | **앱에서 구글 로그인이 꺼져 있다** — `GOOGLE_AUTH.webClientId = ''`(`config.ts:84`) 라 버튼이 안 그려진다(`isGoogleLoginConfigured`). 서버 `GOOGLE_CLIENT_IDS` 도 비었을 것. 8/18 "코드 버그로 미동작"은 이 상태를 말한 것으로 보인다 | 데이터 보안·앱 액세스 양식에서 "Google 로그인"을 **선택하지 않은 채** 두는 게 지금 코드와 맞다. 켜려면 Google Cloud OAuth 클라이언트(웹·안드로이드) 발급 → `config.ts` + Railway `GOOGLE_CLIENT_IDS` → 빌드 |
| 4 | 개인정보처리방침 URL 공개 | **해결됨** — `landing/privacy.html` 이 dubly.co.kr 에 정적으로 있다(Netlify, `_redirects` 로 `/privacy`). 8/18 의 "구글 문서 링크 공개 설정" 항목은 낡았다 | 등록정보의 개인정보처리방침 URL 이 `https://dubly.co.kr/privacy` 인지 |
| 5 | 스크린샷 재촬영 (실제 기록이 찬 화면) | `store/shots/` 에 `02~05` 네 장. `01-photo.png`(사진 기록) 한 장이 빠졌고, 럽슐랭 장(`04-place`)은 `STORE_LISTING` §5 대로 테스트 장소가 찍혀 있어 재촬영 대상 | 등록정보 스크린샷이 9/17 이후 갱신됐는지 |
| 6 | 앱 액세스(리뷰어 계정) | `playreview@doubly-app.com` 이 문서에 있다. 커플 기능은 **두 계정**이 연결돼야 보이므로 리뷰어용 상대 계정과 초대코드 안내가 함께 있어야 한다 | 앱 액세스 → 로그인 안내에 상대 계정·연결 절차가 적혀 있는지 |
| 7 | 데이터 보안 양식 | 완료로 기록(8/18). 그 뒤 추가된 수집 항목: **결제(구독·소모성)**, 우리 이모지 **사진(처리 후 삭제)**, 통화(마이크). 9/25 결제 추가로 "구매 내역" 항목이 맞는지 | 데이터 보안 → 결제·사진·오디오 항목 |
| 8 | 콘텐츠 등급 · AI 기능 | 앱 안 AI(식단 분석·이모지)는 콘텐츠 등급 설문의 대상. 스토어 이미지는 생성형 AI 아님("라벨 지정 안함" 판단, 8/18) | 등급 설문에 AI 생성 콘텐츠 항목 답변 |
| 9 | 인앱 상품 | `pro_monthly` 등록(9/17). **오늘 추가된 `pro_yearly`·`emoji_set_1` 은 미등록** — `GOOGLE_PLAY_BILLING.md` §1-2·§1-3 | 구독·인앱 상품 목록 |
| 10 | 버전 | `app.json` version `1.0.4`. 결제 기능은 JS 변경이라 업데이트로 나가지만, **구독 상품이 붙은 첫 심사는 결제가 동작하는 빌드**여야 한다(애플과 달리 구글은 강제하진 않는다) | 프로덕션에 올라간 versionCode (`node scripts/check-play-track.mjs`) |

## 정리 — 실제로 남은 것

- **사람이 해야 하는 것**: 1(테스터 모집·14일), 3(구글 로그인을 켤지 결정 — 안 켜면 양식만 맞추면 끝),
  6(리뷰어 상대 계정), 7(데이터 보안 양식 갱신), 9(상품 둘 등록).
- **저장소에서 할 수 있는 것**: 2·5 는 `01-photo.png` 한 장을 찍어 `store/shots/` 에 넣고 `python3 store/compose.py store/shots`
  를 돌리면 폰·태블릿 여섯 크기가 다 나온다. 럽슐랭 장은 테스트 장소를 지운 뒤 다시 찍는다.
- **낡아서 지워도 되는 것**: 4(개인정보 링크 공개 설정 — 정적 페이지로 해결됨).

`FREE_TIER_AND_ADS.md` 의 8/18 표는 이 문서로 대체한다.
