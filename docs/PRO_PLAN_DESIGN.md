# PRO 요금제 설계 — 2026-08 진단 리포트 신규 기능 반영

[Feature.java](../backend/src/main/java/com/fitto/common/plan/Feature.java) 한도 숫자의
**근거와 설계 의도**를 정리한 문서입니다. 실제 판정 코드는 그 파일 하나가 유일한 출처이고,
이 문서는 "왜 이렇게 나눴는지"를 남겨 나중에 숫자를 조정할 때 참고합니다.

> ⚠️ 여기 적힌 FREE/PRO 숫자는 [Feature.java](../backend/src/main/java/com/fitto/common/plan/Feature.java)
> 상단 주석과 마찬가지로 **실사용 분포(p60~p75) 측정 전 자리표시자**입니다. 값을 조정할
> 곳은 그 파일 하나이고, 이 문서는 그 뒤에 맞춰 갱신합니다.

## 나누는 원칙 4가지

1. **관계 코어 루프는 절대 유료화하지 않는다** — 채팅, 가상 터치 기본 3종, 무드, 오늘의
   질문(시스템), 기록 자체(운동·식단), D+, 스트릭, **통화·영상통화**. 이걸 막으면 커플
   앱이 성립하지 않고, 무료 코어가 튼튼해야 그 위의 PRO를 팔 사용자가 남는다.
2. **훅은 무료로 맛보기** — AI 음식 사진처럼 "이 앱을 왜 쓰는지" 보여주는 기능은 무료에
   적은 횟수로 남긴다(`Feature.java` 기존 원칙).
3. **데이터가 쌓일수록 가치가 커지는 것 = PRO** — 추억, 전체 기간 통계, 무드 캘린더,
   연간 리캡. 지불 의사와 같은 곡선.
4. **원가형(Cloudinary·Gemini·통화 분)은 PRO도 상한을 둔다** — "무제한"은 판매 문구고,
   실제로는 항상 사고 방지선이 있다.

## 신규 게이트 — Feature.java 등재 완료

아래는 이번에 `Feature.java`에 새로 등재한 항목이다. 전부 **아직 기능 자체가 구현되지
않은 상태**로 미리 등재해둔 것 — 값이 근거 없는 자리표시자인 것은 기존 `MEMORIES`·
`FULL_STATS`·`PREMIUM_STICKER`와 같은 성격이다(구현 시 게이팅을 빠뜨리지 않기 위한 선등록).

| Feature | FREE | PRO | 커플 판정 |
|---|---|---|---|
| `AI_NEXT_MEAL` (다음 끼니 AI 제안) | 1회/주 | 5회/일 | 개인 (AI_DIET_COACH와 동일) |
| `CUSTOM_EXERCISE` (커스텀 운동 종목) | 3개 | 30개 | 개인 (WORKOUT_ROUTINE과 동일) |
| `CHALLENGE_ACTIVE` (진행 중인 대결) | 1개 | 3개 | 커플 |
| `COOP_GOAL_ACTIVE` (합산 목표) | 1개 | 3개 | 커플 |
| `MOOD_CALENDAR_FULL` (무드 캘린더 전체 기간) | ❌ (최근 30일만 무료) | ✅ | 커플 |
| `WORKOUT_RECOVERY_FULL` (회복 부위 전체 보기) | ❌ (최근 부위 한 줄만 무료) | ✅ | 커플 |
| `ANNIVERSARY_RECAP` (우리의 1년 리캡) | ❌ (티저 2~3장만, 화면 자체 구현) | ✅ | 커플 |
| `LIVE_WORKOUT_CALL` (같이 운동 라이브) | ❌ | ✅ | 커플 |
| `STREAK_REPAIR` (스트릭 복구권) | ❌ | 2회/월 | 커플 |
| `WORKOUT_BOOSTER` (운동 부스터) | ❌ (고정 3문구는 계속 무료) | 3회/주 | 커플 |
| `CUSTOM_QUESTION` (서로에게 묻는 질문) | 1개/주 | 3개/일 | 커플 |
| `VOICE_MESSAGE` (채팅 음성 메시지) | 5개/일 | 100개/일 | 커플 |
| `PUBLIC_GUIDE_LINK` (럽슐랭 공개 가이드) | 1개 | 무제한 | 커플 |
| `CSV_EXPORT` (기록 내보내기) | 1회/월 | 1회/주 | 커플 |

**새 Feature를 추가할 때는 반드시 두 곳을 같이 고친다**:
[`Feature.java`](../backend/src/main/java/com/fitto/common/plan/Feature.java)와
[`frontend/src/types/index.ts`의 `FeatureKey`](../frontend/src/types/index.ts) — 두 목록이
정확히 일치하는지 `PlanFeatureSyncTest`가 검증한다. 하나만 고치면 테스트가 빨간불로 잡아준다.

## 기존 게이트를 재사용한 것 (새 Feature 안 만듦)

새 제안 중 아래 항목들은 기존 게이트가 이미 정확히 같은 모양이라 **새 enum을 안 만들고
재사용**하기로 했다 — enum이 늘어날수록 "이게 저거랑 뭐가 다르지"가 반드시 생긴다.

- **심화 영양 통계 · 물/단식 습관 리포트 · 체중·칼로리 수지 추이** → `FULL_STATS` 하나로
  통합. 전부 "무료는 최근 구간만, PRO는 전체 기간"이라는 동일한 규칙이고, 도메인별로
  나누면 한도만 갈라지고 유지보수가 늘어난다.
- **시즌 스티커·무드팩** → `PREMIUM_STICKER`(이미 선언돼 있으나 어디서도 판정하지 않는
  상태 — 이번에 콘텐츠를 추가하면 그대로 "실동작"시키면 된다).
- **커플 운동 약속** → `CALENDAR_EVENT`(캘린더 일정의 한 종류일 뿐).

## 전면 무료로 두기로 한 것 (Feature 등재 안 함)

수익화 훅이 아니라 **커플 코어 루프이거나, 막으면 오히려 손해인 것들**이다. Feature에
등재하지 않는다는 것 자체가 "이건 절대 잠그지 않는다"는 선언이다.

- 커플 응원 리액션, 서로에게 묻는 질문 중 시스템 질문(기존), 우리 대화 갤러리, 커플 오늘
  영양 공유, 음식 이름 검색(공공 DB — 오히려 Gemini 원가를 줄여준다), 대결 자동판정·
  통산전적·리매치, 자랑 카드 이미지 공유, 럽슐랭 가이드 검색·필터, 알림 인프라·재방문
  리마인드·오프라인 안정화·PWA.
- **주간 결산(`WEEKLY_RECAP`) 숫자 요약을 무료로 열어주는 안도 검토했으나 보류**했다
  (AI 편지만 PRO로 유지하고 숫자는 무료로 열면 "AI 편지가 궁금해서 결제" 동선이 생긴다는
  논리). 다만 이미 `PlanGatingFlowTest`가 "무료는 잠김"을 검증하는 기존 결정이라, 이번
  작업 범위를 벗어나는 정책 변경으로 판단해 **되돌렸다**. 바꾸려면 별도로 결정하고 그
  테스트도 같이 고쳐야 한다.

## 통화·영상통화 — 구조가 달라서 별도 취급

[PLAN.md](../PLAN.md#feature-통화--영상통화-관리형-sdk--stream-video)의 통화 기능은
**Feature/Quota 체계에 그대로 넣을 수 없다.** 이유:

- 기존 게이트는 전부 "**사용자 1명 × 기간(일/주/월)**"으로 세는 카운터(`UsageCounter`)다.
- 통화는 "**커플 1쌍 × 연속된 분(分)**"을 누적해야 하는 완전히 다른 모양의 계량이다.
  거는 사람 기준으로 세면 상대방은 무제한으로 보이는 이상한 상태가 된다.

그래서 이번엔 Feature enum에 등재하지 않고 설계만 남긴다:

| 항목 | 방향 | 이유 |
|---|---|---|
| 음성·영상 통화 자체 | **전면 무료, 게이팅 없음** | 카카오톡·페이스타임이 완전 무료 대체재라 유료화하면 그냥 이탈한다. 통화는 파는 물건이 아니라 무료로 까는 인프라 |
| 월 통화 시간 안전망 | FREE 커플당 15시간/월, PRO 커플당 60시간/월 (제안값) | 판매 문구가 아니라 `AI_TOTAL`과 같은 **사고 방지선** — Stream Video 무료 티어(Maker Account 기준 월 333,000 참가자-분)를 소수 헤비 유저가 독식하는 걸 막는다. 초과 시 402(업셀)가 아니라 **429**(PlanGuard의 "유료 사용자 한도 초과 = 남용 방지" 원칙과 동일) |
| `LIVE_WORKOUT_CALL`(같이 운동 라이브) | **Feature.java에 이미 등재** | 통화+운동 세션 결합은 사용 횟수 기반(blocked/unlimited)이라 기존 체계로 표현 가능. 카카오톡이 못 따라하는 Doubly 고유 가치라 여기가 진짜 PRO 상품 |

**구현 시 필요한 것**: `UsageCounter`와 별개로 "커플 단위 분(分) 누적 카운터"를 새로
만들어야 한다. 판정 시점도 다르다 — 기존 게이트는 사용 *전* 확인(`PlanGuard.consume`
선차감)이지만, 통화는 **종료 시점에 사용량을 기록**해야 한다(통화 중간에 한도가 찼다고
끊어버리면 최악의 경험이 된다).

## 판매 문구가 되는 축 6개 (요약)

1. **AI 다 열림** — 끼니 제안·식단 코치·맛집 추천·주간 편지·여행 일정
2. **기록의 역사가 열림** — 추억, 전체 통계, 무드 캘린더 전체, 1년 리캡
3. **더 많이** — 루틴 30개, 맛집 핀 무제한, 사진 1000장, 음성 메시지, 통화 시간
4. **스트릭 보험** — 복구권 월 2회
5. **꾸미기** — 시즌 스티커, 배경, 프리미엄 터치
6. **커플 단위 혜택** — 한 명만 결제해도 둘 다 PRO(`PlanResolver.resolveForRelation` —
   "커플당 결제 1건" 모델, 이미 구현됨)
