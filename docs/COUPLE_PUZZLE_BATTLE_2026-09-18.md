# Dubly 커플 퍼즐 대전 — 설계 결정 (뿌요뿌요형)

작성: 2026-09-18 · 상태: **단계 0~6 구현(혼자 연습 + 라이브·고스트 대전 + 핸디캡·결과 카드) — §11·§12 참고. 실기기 미검증**

선행 문서: `docs/COUPLE_GAMES_DESIGN_2026-09-09.md`(스도쿠·오목),
`docs/COUPLE_GAMES_EXPANSION_2026-09-14.md`, `docs/CATCH_MIND_2026-09-14.md`

> 이 문서는 같은 날 작성된 **발사형(퍼즐보블) 안을 대체**한다. 발사형을 폐기한 이유와
> 초안의 재사용 자산 주장이 코드와 어긋난 지점은 §7·§8에 남긴다.

## 1. 무엇을 만드는가

뿌요뿌요 계열 **낙하형 연쇄 퍼즐의 커플 대전** 모드. 2개짜리 조각이 떨어지고, 같은 색
4개 이상이 붙으면 사라지며, 위의 조각이 내려오면서 **연쇄(rensa)** 가 일어난다. 연쇄를
터뜨리면 상대 보드에 방해 조각이 날아간다.

## 2. 확정된 결정

### 2-1. 낙하형이다 (발사형 폐기)

초안은 발사형(퍼즐보블)이었고 근거는 "한 손 엄지·세로 화면"이었다. 낙하형도 **좌우 드래그
+ 탭 회전**으로 한 손 엄지가 성립하므로 그 근거는 유지된다. 뒤집은 실질적 이유는 §7.

### 2-2. 승부 구조 — 대전 (방해 전송)

협동(보드 공유)·점수 비교 대신 대전. 협동은 게임 로직이 동일해 승패 처리만 바꾸면 되는
후순위 확장 후보로 남긴다.

### 2-3. 상쇄 시스템은 코어 (필수)

방해 조각이 대기 중일 때 내 연쇄가 터지면 상쇄되고 초과분만 반격으로 나간다.

- **근거**: 상쇄가 없으면 선(先)연쇄 측의 굳히기로 경기가 끝나 역전이 소멸한다.
  뿌요뿌요 통이 오락실에서 성립한 이유가 이 시스템이다. **원조로 오면서 이 근거가
  더 단단해졌다** — 상쇄는 이 장르의 기본 문법이고 플레이어의 기대에 맞는다.
- 구현: 받은 방해를 즉시 보드에 넣지 않고 **대기 큐**에 넣는다. 내 연쇄 발생 시 큐에서
  차감하고, 남은 잔량이 있을 때만 다음 조각 착지 후 보드에 투입한다.

### 2-4. 연쇄 규칙 — 확정됨

**낙하한 조각은 재매치를 유발한다.** 초안에서는 "착수 전 확정 필요"로 남아 있던 항목인데,
낙하형에서는 그것이 곧 연쇄의 정의이므로 자동으로 확정된다. 초안의 미결 항목 하나가
장르 변경으로 소멸했다.

### 2-5. 네트워크 — "결과 보드"를 보낸다

조작 입력만 보내 양쪽이 각자 시뮬레이션하면 부동소수점·타이밍 차이로 보드가 갈린다.

- **둔 쪽이 착지·매치·낙하·연쇄까지 전부 계산**해 최종 보드 상태를 전송한다.
- 받는 쪽은 조작을 보고 연출만 재생하고, **보드는 받은 값을 신뢰한다**.
- 커플 앱이므로 치팅 대응이 불필요 → 서버 권위와 결정론 요구사항을 통째로 제거할 수 있다.
  이것이 최대 단순화 지점이며, 낙하형이 발사형보다 결정론 요구가 큰 것도 이걸로 상쇄된다.
- 페이로드: `{ 조각색2, 착지열, 회전, 결과보드, 전송방해량, 잔여대기큐, 시퀀스 }`

### 2-6. 비동기 성립 — 고스트 대전

대전 + 실시간 방해 전송은 동시 접속을 강제한다. 초안이 플랫포머를 보류한 바로 그 이유라
그대로 두면 안 된다.

- 플레이 시 `[경과ms, 착지열, 회전, 조각색2]` 시퀀스를 기록한다(한 판 100수 기준 수 KB).
- 상대는 나중에 혼자 시작해도 그 타임라인대로 방해를 받는다 → 체감은 실시간 대전.
- **둘 다 접속 중이면 동일한 이벤트를 라이브로 흘린다. 이벤트 포맷이 같으므로 코드는 한 벌.**

### 2-7. 실력차 핸디캡 (기능 요구사항)

커플은 실력차가 큰 경우가 많고, 대전물에서 실력차는 이탈로 직결된다. 채팅 외 탭이 잘
쓰이지 않는 현 상태의 반복을 막기 위한 필수 장치다.

| 장치 | 내용 |
|---|---|
| 자동 핸디캡 | 최근 전적 기반 방해 전송 계수 조정(예: 3연패 시 피격량 70%) |
| 비대칭 시작 | 약한 쪽 시작 보드 1줄 감소 |
| 패배 보상 | 패배해도 최고 연쇄 기록을 채팅에 카드로 전송 |

핸디캡을 숨기지 않고 노출한다(커플 맥락에서는 오히려 자연스럽다).

## 3. 초안에서 틀렸던 것 — 코드 대조 결과

초안의 "재사용 자산" 5개 중 **2개는 이 저장소에 없고, 1개는 용도가 다르다.** 그래서
3일 일정이 성립하지 않는다. 확인 근거를 남긴다.

### 3-1. 똥피하기가 이 저장소에 없다

```
grep -rln "똥피하기|dodge|Dodge" → backend/.../CatchMindWords.java 뿐 (제시어 목록의 "똥")
frontend/src/screens/home/ → CatchMind · CoupleCalendar · CoupleConnect ·
                             DailyQuestion · Home · MiniGames · Omok · Sudoku
```

따라서 초안이 재사용 자산으로 적은 **① 게임 루프 원칙(SharedValue 프리미티브 분리,
dt 클램프 50ms, runOnJS 최소화) ② 화면 흔들림 감쇠 로직** 둘 다 실재하지 않는다. 다른
프로젝트에 있거나 만들어지지 않았다. **연출과 게임 루프는 0에서 시작한다.**

`react-native-reanimated@4.3.1` 은 있다(`frontend/package.json:55`). Skia 는 없다 —
캐치마인드가 `react-native-svg` 로 간 이유와 같다(웹 번들에 CanvasKit WASM 수 MB).
따라서 보드 렌더는 **Reanimated + View 또는 react-native-svg** 로 간다.

### 3-2. 오목 WebSocket 은 "전송 계층"이 아니다

```java
// common/event/CoupleEvent.java
/** 민감 데이터는 싣지 않고 타입만 보내, 수신측이 인증된 REST 로 다시 조회한다. */
public record CoupleEvent(String type) { ... }

// common/event/CoupleEventPublisher.java:18
public void publish(Long relationId, String type) {
    messagingTemplate.convertAndSend("/sub/couple/" + relationId, new CoupleEvent(type));
}
```

오목은 수를 소켓으로 보내지 않는다. `CoupleEvent.GAME` 이라는 **타입 한 개**를 보내고
클라이언트가 REST 로 재조회한다. `OmokService` 의 `convertAndSend` 는 승리 시 채팅 카드
하나뿐이다(`OmokService.java:259-260`).

그래서 §2-5 의 페이로드를 "오목 한 수와 같은 규모"로 이 채널에 실을 수 없다. 발당 재조회
왕복이 붙고 그것이 게임의 지연이 된다.

**다만 STOMP 전체가 막힌 건 아니다.** 채팅은 이미 `/sub/rooms/{relationId}` 로 전체
페이로드(`ChatMessageResponse`)를 밀어 넣는다. 즉 페이로드 채널은 아키텍처상 정상이고,
**의도적으로 페이로드가 없는 것은 `CoupleEvent` 쪽**이다. 따라서 채팅 패턴을 따라
새 채널을 만든다.

| 방향 | 경로 | 비고 |
|---|---|---|
| 발행 | `/pub/games/{relationId}` | `/pub/chat/{relationId}` 와 같은 모양 |
| 구독 | `/sub/games/{relationId}` | 클라이언트는 `chatSocket.ts` 의 `register()` 재사용 |

`common/security` 의 STOMP 채널 인터셉터가 같은 JWT 로 인증하므로 인증은 추가 작업이 없다.
클라이언트도 `subscribeRoom`(`src/api/chatSocket.ts:224`)과 같은 형태로 한 줄이다.

### 3-3. Flyway 작업이 일정에 없었다

`couple_games` 는 JPA 단일 테이블 상속이고 `game_type VARCHAR(20)` 이므로 **`GameType`
enum 값 추가는 마이그레이션이 필요 없다**. 하지만 대전은 저장할 것이 새로 있다.

기존 3게임이 `couple_games` 에 쌓이는 것과 달리 **대전은 한 판에 두 사람의 보드·기보를
들고 판 수도 훨씬 많다.** 행 크기와 증가 속도가 다르므로, 기보를 몇 판까지 보관할지
(예: 최근 N판만) 착수 시 정한다 — 무제한 누적은 `Feature.COUPLE_GAME` 이 한도가 없어서
(아래 §5) 아무도 막지 않는다.

**다음 번호는 V95 다** (2026-09-18 확인: `origin/main` 최대 94, 미병합 브랜치 선점 없음,
중복 없음). CLAUDE.md 7절 절차로 **착수 시점에 다시 센다** — 그 사이 다른 세션이 집을 수 있다.

V93(캐치마인드)의 패턴을 그대로 따른다 — 전부 `ALTER TABLE couple_games ADD COLUMN`,
**nullable**(다른 game_type 행은 이 값이 없다). V87 이 오목 때문에 스도쿠 컬럼의 NOT NULL
을 푼 것과 같은 이유다.

| 컬럼 | 용도 |
|---|---|
| 보드 2개 | 각자의 판. 문자열 1개씩(스도쿠 `board VARCHAR(81)` 과 같은 발상) |
| 대기 큐 2개 | 상쇄 잔량. `INTEGER` 2개 |
| 기보 | 고스트 재생용 `[경과ms, 열, 회전, 색2]` 시퀀스. `TEXT` (V93 `strokes` 와 같은 취급) |
| 승자 · 최고연쇄 | 결과 카드와 핸디캡 계산용 |

`JSONB` 금지(CLAUDE.md 4절)이므로 기보는 문자열로 인코딩한다 — 캐치마인드 `strokes` 와
같은 규칙(구분자 2단계, 서버는 해석하지 않고 형식만 검증).

**Purger 는 손댈 필요가 없다.** 같은 `couple_games` 테이블이라
`RelationRecordPurger.java:89` 의 `delete from couple_games where couple_id = :rid` 한 줄이
그대로 거둔다(캐치마인드가 테스트로 이미 고정: `관계_기록_삭제에_캐치마인드_판이_포함된다`).
**새 테이블을 만들면 이 면제가 사라진다** — 그것이 단일 테이블을 유지하는 이유다.

## 4. 기술적 버그 위험 지점

### 4-1. 초안에서 사라진 위험

초안이 "버그 대부분이 여기서 발생"이라 꼽은 **오프셋(육각) 그리드가 없어졌다.** 뿌요뿌요는
직사각 그리드라 이웃 계산이 `±1`, `±width` 로 끝난다. 발사형의 궤적·충돌·스냅도 통째로
빠진다. **이것이 장르를 뒤집은 가장 큰 이득이다.**

### 4-2. 남는 위험

1. **부유 클러스터 낙하 순서** — 반드시 *매치 제거 → 중력 적용 → 재매치 판정* 순서.
   역순으로 구현하면 정상 덩어리가 떨어진다. 낙하형은 열 단위 중력이라 발사형의
   flood fill 보다 단순하다.
2. **회전 킥(벽 붙은 상태 회전)** — 낙하형 고유의 새 위험. 벽·바닥에 붙은 조각을 회전할 때
   밀어낼지 거부할지 먼저 정한다. 뿌요뿌요는 밀어낸다.
3. **방해 조각의 성질** — 매치에 참여하지 않고, 인접 색이 사라질 때 함께 사라지는가.
   뿌요뿌요는 **함께 사라진다**(방해 제거가 유일한 해소 수단). 이 한 줄을 안 정하면
   난이도가 통째로 달라진다.
4. **중력 타이밍 기준** — 시간이 아니라 **수(手) 기준**으로 진행한다. 비동기/고스트 대전과
   양립하려면 필수다.
5. **동시 종료** — 둘이 동시에 K.O. 되는 경우의 승패. 시퀀스 번호로 먼저 끝난 쪽을 패배로
   확정한다(§2-5 페이로드의 `시퀀스`가 이 용도도 겸한다).

위 로직은 전부 **렌더링 없는 순수 함수**로 분리 가능하며 단위 테스트 대상이다. 프론트에는
테스트 러너가 없으므로(CLAUDE.md 6절) **순수 로직은 백엔드에 두고 Java 테스트로 고정하거나**,
프론트에 둘 경우 `scripts/verify-*.mjs` 패턴의 검증 스크립트를 함께 만든다. 후자를 택한다면
그 스크립트가 사실상의 테스트다 — `verify-spellcheck.mjs` 가 맞춤법 모듈에 하는 역할과 같다.

## 5. 실제 재사용 자산 (검증됨)

| 자산 | 상태 |
|---|---|
| `couple_games` 단일 테이블 + `GameType` enum | ✅ 마이그레이션 없이 enum 값 추가 |
| `RelationRecordPurger` | ✅ 손댈 필요 없음 — `RelationRecordPurger.java:89` 가 `couple_games` 를 통째로 지운다(§3-3) |
| `PlanGuard` + `Feature.COUPLE_GAME` | ⚠️ **게이팅 아님** — FREE·PRO 모두 `Quota.unlimited()`(`Feature.java:202`). `require` 를 부르는 건 사용량 계측(`FEATURE_USED`) 때문이다. 대전에 한도를 걸 생각이라면 새 `Feature` 가 필요하다 |
| `GameStreakService` | ✅ 기존 3게임과 같은 편입 |
| `MessageType.GAME_CARD` + `postSystemCard` | ✅ 결과·패배 보상 카드 그대로 |
| STOMP 인증(`common/security` 채널 인터셉터) | ✅ 새 채널도 같은 JWT |
| `chatSocket.ts` 의 `register()` | ✅ 새 구독 한 줄 |
| `MiniGamesScreen` 카드 목록 | ✅ 진입점(§6) |
| `react-native-reanimated@4.3.1` | ✅ 있음 |
| 게임 루프 원칙 · 화면 흔들림 | ❌ **없음**(§3-1) |
| 오목 "전송 계층" | ⚠️ **용도 다름**(§3-2) |
| 매치메이킹 | ✅ 불필요(상대 고정) |

## 6. 진입점 — 확정

초안은 "미정, 채팅 탭 내 초대/결과 카드 유력"이었다. **`MiniGamesScreen` 카드 추가로
확정한다** — 기존 3게임이 모두 그 형태이고(`MiniGamesScreen.tsx:201-221`),
`navigation.navigate('CatchMind')` 처럼 한 줄이다. 채팅은 **결과 카드**를 받는 쪽이다
(`GAME_CARD`, 캐치마인드·스도쿠·오목과 동일). 즉 둘 중 하나가 아니라 **둘 다이고 역할이 다르다.**

## 7. 왜 뿌요뿌요인가 — 발사형을 뒤집은 근거

| | 발사형(초안) | 낙하형(확정) |
|---|---|---|
| 그리드 | 오프셋(육각). 초안이 직접 "버그 대부분" 지목 | 직사각. 이웃이 `±1`/`±width` |
| Day 1 물량 | 궤적·충돌·스냅 + 육각 이웃 | 착지·회전킥 + 직사각 이웃 |
| 상쇄 근거 | 뿌요뿌요를 인용 | 원조 그 자체 |
| 연쇄 규칙 | 미결(착수 전 확정 필요) | 장르 정의상 자동 확정 |
| 한 손 엄지 | 조준 드래그 | 좌우 드래그 + 탭 회전 (성립) |
| 새 위험 | — | 회전 킥, 방해 조각 성질 |

위험 하나가 사라지고 두 개가 생겼지만, 사라진 것이 초안 스스로 "버그 대부분"이라 꼽은
항목이고 새로 생긴 둘은 **착수 전 한 줄로 확정 가능한 규칙 선택**이다(§4-2 2·3번, 둘 다
뿌요뿌요의 기존 답이 있다). 그래서 낙하형이 기술적으로 더 안전하다.

## 8. 보류·폐기한 대안

### 발사형 퍼즐보블 (폐기)
§7 표 참고. 육각 그리드 위험과 미결 연쇄 규칙을 그대로 안고 가야 한다.

### 실시간 커플 플랫포머 (보류)
퍼즐 완료 후 재검토.
- 검토 결론: 캐릭터 간 상호작용(밟기·같이 누르기)이 **없다면** 각자 로컬 권위 + 상대 위치
  10~15Hz 보간으로 성립. 시드 기반 결정론적 맵 생성으로 맵 전송 0. 상호작용이 **있다면**
  결정론 물리 + 롤백 넷코드가 필요해 별도 프로젝트 규모.
- **보류 사유**: 실시간이라 둘 다 동시에 폰을 보고 있어야만 성립한다. 커플 앱 사용 패턴상
  성사율이 낮고, 네트워크가 실패하면 결과물이 남지 않는다.
- 되살릴 경우 필수: 3초 무패킷 시 양쪽 동시 일시정지(미구현 시 상대가 정지한 채 혼자
  달리는 상태 발생).

### 협동 모드 (후순위 확장)
보드 하나를 번갈아 두는 방식. 커플 앱 정체성에는 더 맞으나 대전을 우선. 게임 로직이
동일하므로 승패 처리만 바꾸면 된다.

### 한 폰 2인 플레이 / 점수만 비교 (미채택)
전자는 떨어져 있는 커플에게 무의미, 후자는 상호작용이 약해 대전물로 성립하지 않는다.

## 9. 개발 단계 (수정판)

초안의 3일은 §3의 세 구멍(똥피하기 부재 · 소켓 페이로드 채널 · Flyway)을 계산하지 않았다.

| 단계 | 내용 |
|---|---|
| 0 | **Flyway V95**(착수 시 번호 재확인) + `GameType` 값 추가 + 엔티티·Purger 확인 |
| 1 | 직사각 그리드·중력·매치·재매치 **순수 함수** + 단위 테스트(§4-2가 테스트 목록) |
| 2 | 상쇄 대기 큐 + 방해 투입 + 승패 판정 + 회전 킥 |
| 3 | **`/pub/games/…` · `/sub/games/…` 채널 신설**(서버 + `chatSocket.ts`) |
| 4 | 기보 기록·고스트 재생(단계 3과 같은 이벤트 포맷) |
| 5 | 게임 루프·연출 **0에서**(SharedValue 분리, dt 클램프, 화면 흔들림) + 진입점 카드 |
| 6 | 핸디캡 + 결과·패배 보상 `GAME_CARD` |

중간에 막혀도 **단계 2까지면 로컬 1인 플레이로 출시 가능하다.** 플랫포머 안은 네트워크가
실패하면 아무것도 남지 않는 구조였고, 이것이 퍼즐을 우선한 핵심 근거다 — 장르를 바꿔도
이 근거는 그대로다.

**3일은 아니다.** 단계 3·5가 초안에 없던 물량이다.

## 10. 미결 항목

| 항목 | 내용 |
|---|---|
| ~~방해량 공식~~ | **확정(§11-2)** — 뿌요뿌요 통 공식 그대로 |
| 핸디캡 계수 | 구체 수치 미정(§2-7 의 70% 는 예시). 자리는 `scoring.ts` 의 환산 결과에 계수를 곱하는 곳 |
| ~~보드 크기~~ | **확정(§11-2)** — 6×12 + 숨은 1줄. 셀 크기는 화면 폭·높이 중 작은 쪽에 맞추고 18~44px 로 묶음. 실기기 미확인 |
| 렌더 방식 | **View 로 시작(§11-3)**. 절대 위치 View 72개 + 조각·고스트. 60fps 는 실기기 미확인 — 끊기면 그때 Reanimated |
| ~~순수 로직 위치~~ | **확정(§11-1)** — 프론트 TS + `scripts/verify-puyo.mjs` |
| 오디오 | 연쇄 효과음 유무 미정. 있으면 에셋 라이선스 확인 필요. 지금은 햅틱만(1~2연쇄 light, 3연쇄+ medium, 끝에 success) |
| 아이콘 | 조작 버튼이 `restart`·`chevron-down` 을 쓰는 건 폰트 서브셋에 있는 것만 골라서다(§11-4). 원하는 아이콘(`rotate-right`·`chevron-double-down`)은 서브셋 재생성이 필요 |
| 실기기 검증 | **미검증.** typecheck·verify·lint 만 통과. 제스처 감도(DRAG_DEAD_ZONE 8px·SWIPE_DOWN_DY 56px)·중력 800ms·연출 260/160ms 는 손으로 맞춰야 하는 값 |
| 일정 | Play Console 비공개 테스트(20명 × 14일)가 크리티컬 패스. 앱 업데이트로 요건은 리셋되지 않으므로 병행 가능 |

## 11. 구현 기록 — 2026-09-18 (단계 1·2·5)

### 11-1. 순수 로직은 프론트에 둔다

§10 미결이었던 항목. §2-5 가 "둔 쪽이 착지·매치·연쇄까지 전부 계산해 결과 보드를 보낸다"는
클라이언트 권위 구조이므로, 로직을 백엔드에 두면 매 착지마다 서버 왕복이 생기거나 같은
로직이 두 벌이 된다. 프론트 TS 로 확정하고 `scripts/verify-puyo.mjs` 가 테스트 역할을 한다
(`verify-spellcheck.mjs` 와 같은 방식 — Node 타입 제거로 .ts 를 직접 실행, **enum 금지**).

```
frontend/src/games/puyo/
  types.ts    상수(6×13, 4색, 4개 매치)·Cell/Board/Piece/PlayerState/LockOutcome
  rng.ts      mulberry32 시드 난수 — 같은 시드 = 같은 조각 순서(대전 공정성·고스트 재생)
  board.ts    중력·무리 탐색·제거·연쇄(resolveChains) · 판 ↔ 78자리 문자열
  piece.ts    이동·회전 킥·낙하·착지
  scoring.ts  점수 공식·방해 환산·상쇄
  engine.ts   createPlayer / moveLeft·Right / rotate / softDrop / hardDrop / receiveGarbage / lock
frontend/scripts/verify-puyo.mjs   74 검사 — §4-2 위험 1~4번이 그대로 항목이다
```

### 11-2. 확정한 규칙 (전부 뿌요뿌요 통 규칙)

| 규칙 | 결정 | 근거 |
|---|---|---|
| 판 | 6×12 + **숨은 1줄**(row 0). 숨은 줄 조각은 매치 불참 | 13단 규칙. 회전 킥으로 조각이 잠시 올라갈 자리 |
| 회전 킥 | 자식이 못 들어가면 축을 반대쪽으로 1칸 민다(벽→떨어짐, 바닥→떠오름). 밀어도 안 되면 거부 | §4-2 2번 — 뿌요뿌요가 미는 쪽 |
| 방해 | 매치 불참. 무리에 인접한 방해는 함께 제거 | §4-2 3번 |
| 점수 | `10 × 색 조각 수 × clamp(연쇄 + 색 + 무리 보너스, 1, 999)`. 방해는 점수에 안 셈 | 통 공식 |
| 방해 환산 | 70점당 1개, 나머지 이월(`carry`) | 통 공식 |
| 상쇄 | 대기 큐에서 먼저 차감, 남는 만큼 전송. **연쇄가 난 수에는 방해가 안 들어온다** | §2-3 |
| 방해 투입 | 연쇄 없는 착지 뒤, 한 수 최대 30개(5줄). 꽉 찬 줄부터, 나머지는 무작위 열. 넘치면 버림 | 통 규칙 |
| 패배 | 다음 조각 생성 자리(열 2, row 1·0)가 막히면 | 통 규칙(3열 12단) |
| 진행 | 시간이 아니라 **수(手)**. 중력 타이머는 화면이 `softDrop` 을 주기적으로 부르는 것뿐 | §4-2 4번 |
| 조각 순서 | 시드 난수. 미리보기 2개 | 대전에서 둘이 같은 순서·고스트 재생 |

독자 공식 대신 원조 공식을 쓴 이유는 상쇄를 코어로 둔 이유(§2-3)와 같다 — 균형이 검증됐고
플레이어 기대와 맞는다. 핸디캡(§2-7)은 환산 결과에 계수를 곱하는 자리다.

### 11-3. 화면 (`screens/home/PuyoScreen.tsx`)

- 규칙 0줄. 입력 → 엔진 함수, `LockOutcome.steps` 를 setTimeout 으로 순서 재생(터짐 260ms →
  낙하 160ms). 연출 중에는 중력·입력을 멈춘다.
- 조작: 판 위 좌우 끌기(칸 단위, 손가락 아래 열을 따라감)·탭 회전·아래로 쓸기 하드드롭 +
  아래 버튼 4개(웹·접근성) + 웹 방향키/스페이스.
- 렌더는 **절대 위치 View** — Skia 없음, svg 도 굳이 필요 없음. 착지 예상 자리(고스트)를 반투명으로.
- `fullScreenGestureEnabled: false` — 캐치마인드와 같은 이유(좌우 끌기가 조작이라 전역 스와이프백과 충돌).
- 내 기록(점수·최고 연쇄)은 AsyncStorage(`doubly.puyoBest`) — `sudokuMemo.ts` 와 같은 판단, 서버 없음.
- 진입점: `MiniGamesScreen` 카드 "연쇄 퍼즐" → `navigate('Puyo')`, 링크 `game/puzzle`.
- UI 이름은 **"연쇄 퍼즐"**. "뿌요뿌요"는 세가 상표라 코드 내부명(`games/puyo`, 라우트 `Puyo`)에만 쓴다.
- `GameReactionBar`·스트릭·`GAME_CARD` 는 붙이지 않았다 — 전부 서버 판(`couple_games`)이 있어야
  하는 것이고, 지금은 로컬 판이다. 단계 0(Flyway)과 함께 붙는다.
- 입력 핸들러가 보는 "지금" 상태(`live`·`livePhase`)는 **모듈 변수**다 — ref 로 두면 그 ref 를
  읽는 핸들러를 `PanResponder.create` 에 넘기는 순간 `react-hooks/refs` 가 막고, 효과에서
  만들면 `set-state-in-effect` 가 막는다. `SudokuScreen.moveSeq`·`OmokScreen.placeSeq` 와 같은 판단.

### 11-4. 걸린 것

- **아이콘 폰트 서브셋**: 이 앱은 쓰는 글리프만 잘라 쓰고(`scripts/subset-icon-font.cjs`) 목록에
  없는 이름은 tsc 에러다. 서브셋 재생성에 Python fontTools 가 필요한데 이 환경에 없어서,
  있는 아이콘(`puzzle`·`restart`·`chevron-down`)으로 대신했다.
- verify 첫 실행에서 2건 실패 — 둘 다 테스트의 전제가 틀린 것(패배 판정 경계를 한 줄 잘못 셈,
  같은 열에만 20수 쌓아 게임오버). 엔진 수정 없음.

### 11-5. 다음 단계에 남기는 것

- 단계 0(Flyway V95 — **착수 시 번호 재확인**) 은 대전과 같이 간다. 로컬 판은 DB 가 필요 없다.
- `LockOutcome` 이 §2-5 페이로드의 재료다: `encodeBoard(state.board)`·`garbageSent`·
  `state.pendingGarbage`·`describeMove(piece)`·`moves`(시퀀스). 소켓 채널만 붙이면 된다.
- `receiveGarbage` 는 이미 있다 — 상대 이벤트를 받아 부르면 다음 착지 뒤 판에 들어온다.
- 고스트 대전(§2-6)은 `createPlayer(seed)` + 기보 `[경과ms, col, rot, 색2]` 재생으로 성립한다.
  경과ms 는 화면이 붙인다(엔진은 시간을 모른다).

## 12. 구현 기록 — 2026-09-18 (단계 0·3·4·6, 대전)

§11-5 에 남긴 그대로 붙였다. `LockOutcome` 이 페이로드가 되고, `receiveGarbage` 가 수신 함수다.

### 12-1. 서버는 심판이 아니라 보관자다

| 경로 | 역할 |
|---|---|
| `POST /games/puzzle` | 판 열기. **시드·핸디캡을 정해** 저장 — 양쪽이 같은 값을 본다. 진행 중 판이 있으면 그걸 돌려준다(오목과 같다) |
| `GET /games/puzzle/current` | 내 결과(`me`)·상대 결과(`partner`, **기보 포함**)·핸디캡 |
| `POST /games/puzzle/{id}/finish` | 내 결과 제출 — 한 판에 한 번(두 번째는 409 `GAME_RUN_ALREADY_SUBMITTED`). 둘 다 내면 승자 확정·COMPLETED·`GAME_CARD` |
| `POST /games/puzzle/{id}/give-up` | 접기 — 어느 쪽이든. 상대가 며칠째 안 치면 다음 판을 못 여는 문제의 출구 |
| `/pub/games/{rid}` → `/sub/games/{rid}` | 수(手) 중계. **저장하지 않는다.** 서버는 발신자(Principal)만 덮어쓰고 활성 관계 구성원인지 확인한다(`PuzzleBattleStompController`). 구독 인가는 `StompAuthChannelInterceptor` 의 접두어 목록에 `/sub/games/` 를 추가한 것으로 끝 |

- **V95** — `couple_games` 에 nullable 컬럼 15개(시드, 양쪽의 점수·최고연쇄·버틴ms·lost·기보·핸디캡, `battle_winner`).
  오목의 `winner` 컬럼을 같이 쓰지 않고 따로 둔 이유: 형제 서브클래스가 한 컬럼을 나눠 쓰는 매핑을 피한다.
- **Purger 변경 없음** — 같은 테이블. `관계_기록_삭제에_대전_판이_포함된다` 테스트로 고정.
- **기보 형식**(`Timeline.java` ↔ `battle.ts`): `"ms,열,회전,축색,자식색,보낸방해,받은방해;..."`. 서버는 형식(값 7개·정수·
  ms 단조 증가·40KB)만 본다. 받은 방해까지 적는 이유는 시드 + 이 기록이면 상대 판 전체를 재구성할 수 있어서다 —
  지금 고스트는 `보낸방해` 열만 쓴다.

### 12-2. 승부 규칙 — 살아남은 쪽 > 오래 버틴 쪽 > 점수 > 무승부

`PuzzleBattleGame.submit` 한 곳에 있다. 라이브에서 상대의 `lost` 이벤트를 받은 쪽은 **죽지 않은 채**
`lost=false` 로 내므로 첫 조건으로 끝난다. 고스트에서 상대의 `survivedMs` 를 넘기면 앱이 그 시각에
`lost=false` 로 낸다. 둘 다 죽었으면(§4-2 5번 동시 종료 포함) 버틴 시간이 곧 시퀀스다.
점수가 아니라 시간을 앞에 두는 이유: 이 장르의 승패는 "상대 판을 채우는 것"이고 점수는 그 수단이다.

### 12-3. 라이브와 고스트는 코드 한 벌(§2-6 확인)

`PuyoScreen` 의 `receive(amount)` 하나로 들어온다.
- 라이브: `/sub/games` 이벤트의 `garbageSent`
- 고스트: 상대 기보를 `ghostSchedule()` 로 풀어 `setTimeout(ms)` 에 같은 `receive`
- 핸디캡(§2-7)은 **받는 쪽이 곱한다**(`applyHandicap`, 반올림 — 1개 × 70% 는 그대로 1개). 보내는 쪽은 모른다.
- 상대가 끝난 시점(`lost` 이벤트 또는 기보의 `survivedMs`)에 내가 살아 있으면 `onOpponentEnded` 가 판을 멈추고 이긴 결과를 낸다.

핸디캡 수치는 §2-7 의 예시를 그대로 채택했다 — 2연패 85%, 3연패부터 70%. 최근 완료 판을 최신부터 세다
이기거나 비긴 판에서 멈춘다(`PuzzleBattleService.handicapFor`). 화면에 "내 핸디캡 · 받는 방해 70%" 로 띄운다.

### 12-4. 결과 카드 = 패배 보상

`GAME_CARD` 한 장에 **양쪽의 최고 연쇄·점수**를 싣는다("연쇄 퍼즐 ○○ 승 🧩 A 3연쇄 1200점 · B 2연쇄 900점").
진 사람의 연쇄가 카드에 남는 것이 §2-7 "패배 보상"의 형태다. 이긴 쪽만 적으면 진 쪽은 채팅에서 지워진다.

### 12-5. 화면의 결정

- 진입 오버레이가 서버 판 상태로 갈린다: 판 없음 → "커플 대전" / 상대만 냄 → "고스트 대전 시작"(상대 점수·연쇄 표시)
  / 내가 냄 → "상대를 기다리는 중"(비활성) + 접기.
- 상대 패널: 미니 판(9px 셀, `PuyoBoard` 재사용)·점수·연쇄·대기 방해·상태(플레이 중/기록 재생 중/가득 참).
  라이브 이벤트의 `board` 를 그대로 그린다 — 받는 쪽은 조작이 아니라 결과 판을 신뢰한다(§2-5).
- 결과 제출이 실패하면 "결과 다시 보내기" 버튼 — 판은 이미 끝나 다시 둘 수 없으므로 값을 들고 재시도만 한다.
- 수 이벤트는 연출과 무관하게 **착지 시점**에 보낸다. 상대 화면에는 연쇄 연출이 없고 결과 판만 바뀐다 —
  §2-5 "받는 쪽은 연출만 재생"은 아직 안 했다(§12-6).
- `publishGameEvent` 는 연결이 없으면 그냥 버린다(채팅의 `publishEnsuringConnection` 과 다르다). 프레임마다 판 전체가
  실리므로 놓친 프레임은 다음 프레임이 덮고, 기다리면 게임 루프가 멈춘다.

### 12-6. 미결·미검증 (이번 세션 이후)

| 항목 | 내용 |
|---|---|
| **실기기 미검증** | 백엔드 테스트(9 + 6건)·프론트 typecheck·verify·lint 만. 라이브 두 대 동시 접속, 고스트 타이밍(백그라운드 진입 시 setTimeout 드리프트), 결과 오버레이 갱신은 손으로 봐야 한다 |
| 상대 화면 연출 | 상대의 연쇄를 내 화면에서 재생하지 않는다(결과 판만 갱신). `LockOutcome.steps` 를 이벤트에 실으면 되지만 페이로드가 커진다 — 필요하면 `cleared` 인덱스만 |
| 백그라운드 | 고스트 타이머·중력이 wall-clock 이라 앱이 뒤로 가면 돌아왔을 때 방해가 몰려온다. AppState 로 일시정지가 답인데 라이브에서는 상대도 멈춰야 한다(플랫포머 안의 "3초 무패킷 시 동시 일시정지"와 같은 문제) |
| 상대 접속 여부 | 라이브인지 고스트인지 시작 전에 모른다 — 안내 문구로만 처리("지금 들어오면 라이브"). 소켓 presence 가 있으면 카드에 "지금 플레이 중" 을 띄울 수 있다 |
| 기보 재구성 | 받은 방해까지 적어 두었지만 재생(복기 화면)은 없다 |
| Feature 한도 | `COUPLE_GAME` 무제한 그대로. 대전 판은 기보 2개(수 KB)라 행이 크다 — 판 수가 늘면 보관 상한(최근 N판) 필요 |
| GameReactionBar | 붙이지 않았다. `gameType='PUZZLE_BATTLE'` 만 넘기면 되지만 대전 중 손이 판에 있어 자리가 애매하다 |
