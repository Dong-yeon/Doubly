-- 커플 캘린더 기간 일정 — 종료일(NULL = 하루 일정, 기존 동작 그대로).
-- 여행(기간 개념)이 캘린더의 진입점이 되면서, 일반 일정도 연휴·출장·부모님 방문처럼
-- 며칠에 걸친 기간으로 등록할 수 있게 한다. 매년 반복 일정(repeat_yearly)은 기간을
-- 갖지 않는다 — 생일·기념일이 원래 용도라 하루로 충분하고, 연말을 걸치는 반복 기간의
-- 발생일 계산 복잡도를 피한다(서비스에서 거부).
-- (H2 호환 구문만 사용 — 테스트가 동일 마이그레이션을 적용한다)
ALTER TABLE couple_events ADD COLUMN end_date DATE;
