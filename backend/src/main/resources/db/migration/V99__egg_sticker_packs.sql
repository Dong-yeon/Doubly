-- 캐릭터 스티커 첫 두 팩 — 삶은달걀 · 맥반석♥달걀 (2026-09-21)
--
-- 스티커 상점의 첫 물건이다. V96 이 판매 구조를 만들었지만 정작 <b>팔 캐릭터가 없었다</b> —
-- 출처가 확인되지 않은 곰돌이와, 그림을 교체하기로 한 더비·블리를 모두 내렸기 때문이다.
--
-- <b>둘 다 0원이다.</b> 캐릭터 스티커는 낱개 구매 상품이지만 첫 두 팩은 무료로 푼다 —
-- 상점에 아무것도 없는 상태로 여는 것보다, 받아 보고 마음에 들면 다음 팩을 사는 쪽이 낫다.
-- <b>되돌릴 수 없는 결정이다</b>: 무료로 준 것을 나중에 유료로 바꾸는 건 새 상품이 아니라
-- 기능 회수로 체감된다(docs/STICKER_PACK_OVERLAP_2026-09-14.md). StickerPackSyncTest 가
-- 그 회귀를 막는다.
--
-- 다음 캐릭터부터 팔려면 여기에 is_pro_only = TRUE + 가격을 준 행을 더하면 된다.
-- 잠금 배지·잠금 해제 줄·결제 검증은 이미 붙어 있어 코드 변경이 필요 없다.
--
-- V96 을 고치지 않고 새 번호를 쓰는 이유: 이미 푸시된 마이그레이션의 내용을 바꾸면
-- flyway_schema_history 의 체크섬이 깨진다(CLAUDE.md 1절).
--
-- V97·V98 이 아니라 V99 인 이유: 병렬 세션이 길막기(wall_race)로 그 둘을 먼저 푸시했다.
-- 이 파일은 아직 안 푸시됐으니 옮기는 건 이쪽이다(CLAUDE.md 1절 — 번호가 충돌하면
-- 아직 푸시되지 않은 쪽을 옮긴다).
--
-- H2/PostgreSQL 양립: 평범한 INSERT 뿐이다.

INSERT INTO sticker_packs (id, title, category, is_pro_only, price) VALUES
    ('EGG_BOILED', '삶은달걀',    'IMAGE', FALSE, 0),
    ('EGG_DUO',    '맥반석♥달걀', 'IMAGE', FALSE, 0);
